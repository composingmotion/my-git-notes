// to ignore tracked file from being updated..
	first add to .gitignore	
	git rm --cached filename // this will keep the repository copy and.. this will keep the working version but it will stop being tracked 


// to ignore a file globally "on the whole computer
	// for linux
		// add file ~/.gitignore_global 
			

//adding a .gitignore files to ignore tracking of certain files
	//negate expressions with ! for example	if you wanted to ignore all *.php files but wanted to include index.php
		*.php		// ignore all .php files
		?index.php	// do not ignore index.php

	//to ignore all files in a directory
		/assets/videos/
